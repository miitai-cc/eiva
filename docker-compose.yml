# RustyClaw Docker Compose
#
# Usage:
#   docker compose up -d          # Start all services
#   docker compose logs -f        # Follow logs
#   docker compose down           # Stop and remove containers
#
# Configuration:
#   Mount your config.toml at /home/rustyclaw/.config/rustyclaw/config.toml
#   or set environment variables for provider keys.

services:
  rustyclaw:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rustyclaw
    restart: unless-stopped
    ports:
      # - "3000:3000"   # SSH transport
      - "39999:39999" # HTTP API (health, tasks, static files)
    volumes:
      - rustyclaw-data:/home/rustyclaw/.config/rustyclaw  # settings / config / workspace root
      - ./vol/skills:/home/rustyclaw/.config/rustyclaw/workspace/skills
      - ./vol/prompts:/home/rustyclaw/.config/rustyclaw/workspace/prompts
      - ./vol/files:/home/rustyclaw/.config/rustyclaw/workspace/files
      - ./vol/assets:/home/rustyclaw/assets                   # served by Salvo API
    environment:
      - RUSTYCLAW_LOG=rustyclaw=info
      # Provider API keys (set in .env or pass via environment):
      # - OPENAI_API_KEY=
      # - ANTHROPIC_API_KEY=
      # - GOOGLE_API_KEY=
      # Messenger tokens:
      # - DISCORD_BOT_TOKEN=
      # - TELEGRAM_BOT_TOKEN=
      # - SLACK_BOT_TOKEN=
    command: ["run", "--ssh-listen", "0.0.0.0:3000"]
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:39999/eiva/backend/api/ver-0.95/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  rustyclaw-data:
